# Placeholder configuration for dataset management

# Provide default directories and augmentation settings for supported datasets.

cifar10:
  root: data/cifar10      # relative path where CIFAR‑10 is/will be stored
  normalize:
    mean: [0.4914, 0.4822, 0.4465]
    std: [0.2023, 0.1994, 0.2010]
  augment: true            # enable random crop and flip for training

fashion_mnist:
  root: data/fashion_mnist
  normalize:
    mean: [0.5]
    std: [0.5]
  augment: false           # minimal augmentation for Fashion‑MNIST